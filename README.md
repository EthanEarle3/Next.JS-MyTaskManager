## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

AI LOG:*Used COPILOT to help build tailwind CSS that would help make the overall page look better
*I used COPILOT to convert my files from TSX to JSX when I was confused with syntax between the two
